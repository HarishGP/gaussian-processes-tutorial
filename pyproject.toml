[project]
name = "gaussian-processes-tutorial"
version = "0.1.0"
description = "One-hour Jupyter tutorial on Gaussian processes with numpy and matplotlib."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.0",
    "matplotlib>=3.10.0",
    "numpy>=2.2.0",
]
